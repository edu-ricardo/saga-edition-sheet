{
  "name": "saga-sheet-app",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "deploy": "npm run build && firebase deploy",
    "preview": "vite preview"
  },
  "devDependencies": {
    "firebase-tools": "^15.6.0",
    "typescript": "~5.9.3",
    "vite": "^7.3.1"
  },
  "dependencies": {
    "firebase": "^12.9.0",
    "sass": "^1.97.3",
    "xlsx": "^0.18.5"
  }
}